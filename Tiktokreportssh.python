# From your machine (post ipwndfu bypass)
scp tiktok_reporter.py root@127.0.0.1:/tmp/ -P 2222

# SSH execute
ssh root@alpine@localhost -p 2222 "
  apt update && apt install python3 python3-requests &&
  cd /tmp && python3 tiktok_reporter.py
"
